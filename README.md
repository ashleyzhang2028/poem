# Ashley 古诗词 · 遗忘曲线记忆法

面向小朋友的**古诗文复习与背诵**应用。覆盖**小学一年级到高中三年级**（人教/统编版教材）共 **242 首**古诗文；
另设独立的**课外必背小古文**学习库（100 篇，点击学习、不做排期，可逐字注音与朗读）。

纯前端单页应用（PWA），**打开即用、无需注册、可离线**，所有学习进度保存在手机/电脑本机浏览器里。

支持**电脑、安卓手机、苹果手机（iPhone / iPad）**；苹果手机上可通过 Safari「添加到主屏幕」当独立 App 使用。

## 📚 两类内容，两套用法

| 内容 | 数量 | 使用方式 | 入口 |
|---|---|---|---|
| 课内古诗文（含长文言文） | 242 首 | 按遗忘曲线排每日任务，点击背诵 | 首页「今日背诵任务」 |
| 课外必背小古文 | 100 篇 | **只列出来，点击学习，不排复习** | 首页「课外必背小古文」卡片 |

为什么小古文要单独一套：
- **篇幅长**：小古文动辄上百字，手机上卡片式弹窗装不下，所以阅读改成**整页阅读器**，可以一路滚下去
- **目标不同**：古诗词求「背下来」，小古文求「读懂」，用同一套遗忘曲线一天排几篇并不合适
- **不打扰主线**：小古文独立成页 `classic.html`，有自己的「已读」标记（与古诗词进度互不干扰），不会占用每日 5 首任务的名额

## ✨ 核心功能

### 1. 在设置里改用户名 + 选年级学期
- 顶部「用户名」输入框：填谁的名字，页面标题就变成「XX古诗词」
  - 留空则使用默认名 `Ashley`；名字同时同步到 iOS 主屏幕应用名与 PWA 清单
  - 最长 12 个字，边输入边生效，无需保存按钮
- 年级/学期选择收进右上角设置弹层，首页不再常驻显示
- 设置内按「小学 / 初中 / 高中」分学段，再选具体年级

> 页面图标（书本、设置、诗词集）均为内联 SVG，不依赖系统 emoji 字体，跨设备显示一致。
>
> 诗词的标题 / 朝代 / 作者 / 正文统一使用**自托管的思源宋体**，图标与评价按钮也全部是内联 SVG，
> 因此苹果、安卓、Windows 上看到的是同一套字形，不会出现 emoji 豆腐块。
- 一键切换「上学期 / 下学期」，只看当前进度对应的教材内容
- 「背诵范围」7 选 1，决定每日任务从哪些内容里抽：

| 选项 | 含义 |
|---|---|
| 本册 | 当前年级 + 当前学期（默认） |
| 本册及之前 | 当前年级当前学期 + 此前所有学期 |
| 小学随机 | 小学 1~6 年级任意学期随机 |
| 初中随机 | 初中 7~9 年级任意学期随机 |
| 小学+初中随机 | 小学与初中任意学期随机 |
| 高中随机 | 高中 10~12 年级任意学期随机 |
| 全部随机 | 全部阶段任意学期随机 |

  - 随机范围每天重新洗牌，同一天内刷新页面保持不变
  - 每日任务从范围内取；范围外已学过且到期的诗仍会排入复习，避免遗忘

### 2. 每日 5 首背诵任务（可调 3/5/8/10 首）
每次进入应用，自动列出**今天要背的诗词名称**（默认 5 首）：

- 🟠 **复习** —— 按记忆曲线到期、今天必须回顾的诗
- 🟢 **新学** —— 还没学过的诗，逐首补齐

不点击就只是"今天要背这 5 首"的清单；**点击某一首**才会展开：

- 诗题
- **作者**
- **朝代**
- 完整正文（保留原文换行、宋体展示，适合跟读）

### 3. 遗忘曲线记忆法自动排期
点开诗词后，如实选择自己的掌握程度，系统自动算出下次复习时间：

| 选择 | 效果 |
|---|---|
| 😄 记住 | 升级到下一记忆阶段，间隔翻倍 |
| 🤔 模糊 | 停留当前阶段，**12 小时**后再复习 |
| 😵 忘记 | 降级一级，**30 分钟**后立刻重来 |

复习间隔序列（天）：

```
当天 → 1 → 2 → 4 → 7 → 15 → 30 → 60 → 120 → 240
```

规律即"先密后疏"：刚记住时反复得快，越牢固间隔越长，用最少次数把诗记进长期记忆。

### 4. 进度可视化
- 今日环形进度（已完成 / 总数）
- 每首诗的记忆阶段与掌握度进度条
- 「本年级本学期全部诗词」折叠面板：诗词总数 / 已学 / 较牢固 / 待复习（选随机范围时改为显示该范围全部诗词）

### 5. 数据管理
- 导出备份（JSON 文件，可换设备导入）
- 导入备份
- 清空进度

## 数据覆盖

| 学段 | 年级 | 首数 |
|---|---|---|
| 小学 | 一年级 ~ 六年级 | 91 |
| 初中 | 七年级 ~ 九年级 | 89 |
| 高中 | 高一 ~ 高三 | 62 |
| **合计** | | **242** |

按教材学期拆分为 24 个（年级 × 学期）分组，每首均含 **标题 / 作者 / 朝代 / 正文**。

不同学段首数：小学 91、初中 82、高中 69。

## 🚀 使用方法

### 方式一：直接打开
用浏览器打开根目录的 `index.html` 即可。

### 方式二：本地起服务（推荐，避免个别浏览器 file:// 限制）
```bash
# Python 3
python3 -m http.server 8080
# 然后访问 http://localhost:8080
```

### 方式三：手机上使用

把整个目录放到任意静态服务器（GitHub Pages / CNB Pages / Nginx 均可），用手机浏览器打开即可。
应用已内置 PWA 能力，**安装到桌面后可以离线背诵**（首次打开需联网加载一次）。

#### 🍎 苹果手机 / iPad（iPhone / iPadOS）

iOS 不会弹「安装」按钮，需要手动添加到主屏幕，步骤：

1. 必须用 **Safari** 打开应用地址
   （微信 / QQ / 钉钉等 App 内置浏览器都不支持「添加到主屏幕」，应用会自动提示您改用 Safari）
2. 点屏幕**底部中间的「分享」按钮**（方框 + 向上箭头 ⬆️）
3. 在弹出的菜单中向下滑动，选择 **「添加到主屏幕」**
4. 右上角点 **「添加」**

完成后桌面会出现「Ashley古诗词」图标（在设置里改了用户名则显示对应名字），点开即为**全屏运行**，没有 Safari 地址栏和工具栏，体验与原生 App 一致。

针对 iOS 已做的适配：

| 项目 | 说明 |
|---|---|
| `apple-mobile-web-app-capable` | 从主屏幕启动时全屏运行，不显示浏览器界面 |
| `apple-mobile-web-app-status-bar-style` | 状态栏样式适配，内容不被状态栏遮挡 |
| `apple-touch-icon` ×4 | 提供 120 / 152 / 167 / 180 四种尺寸，覆盖 iPhone、iPad、iPad Pro，图标清晰不模糊 |
| `viewport-fit=cover` + `env(safe-area-inset-*)` | 适配刘海屏、灵动岛与底部横条，内容不被遮挡 |
| Service Worker | 首次打开后缓存全部资源，**断网也能正常背诵** |
| 输入框 16px | 避免 iOS 聚焦输入框时页面自动放大 |
| 弹层避让 | 弹层打开时自动隐藏安装引导条，不会挡住操作按钮 |

#### 🤖 安卓手机

用 Chrome / Edge 打开，浏览器若支持会提示「安装应用」；也可在菜单中选择「添加到主屏幕」。

#### 💻 电脑

浏览器直接打开即可；也可在浏览器地址栏右侧点安装图标，装成独立窗口应用。

## 📖 课外必背小古文（`classic.html`）

### 内容

100 篇，覆盖需求清单里的全部篇目（《三字经》《弟子规》《世说新语》《山海经》《韩非子》《史记》等），按主题分七组（组内为教材顺序）：

| 分组 | 篇数 | 举例 |
|---|---|---|
| 蒙学经典 | 4 | 人之初、弟子规、菊、莲 |
| 神话传说 | 8 | 盘古开天地、女娲造人、精卫填海、钻木取火、共工怒触不周山 |
| 寓言故事 | 26 | 守株待兔、掩耳盗铃、刻舟求剑、鹬蚌相争、狐假虎威、买椟还珠 |
| 人物故事 | 21 | 司马光、曹冲称象、卧薪尝胆、田忌赛马、孔融让梨、程门立雪 |
| 治学勤读 | 14 | 囊萤夜读、铁杵成针、凿壁借光、王充求学、薛谭学讴、勤读 |
| 诸子论道 | 19 | 生于忧患死于安乐、富贵不能淫、大学之道、虽有嘉肴、诫子书 |
| 志人逸事 | 8 | 咏雪、陈太丘与友期行、管宁割席、荀巨伯探友、身无长物 |

每篇含：**标题 / 出处 / 朝代 / 作者 / 原文 / 白话译文**。

### 页面交互

- **列表**：搜索框（篇名 / 出处 / 作者）+「全部 / 未读」筛选，分组标题带篇数
- **点击**：打开**整页阅读器**（不是卡片弹窗），长文整屏滚动，一眼看到底
- **阅读器**：字号 `A－ / A＋` 五档可调、**逐字注音**、**语音朗读**、译文一键展开收起、右上角「标记已读」
- **进度**：只记「已读 / 未读」，存在 `localStorage` 的 `poem_classic_read_v1`，与古诗词复习进度完全独立
- **离线**：Service Worker 已把 `classic.html`、样式、脚本、100 篇数据一起预缓存

### 和每日任务的关系

小古文**不进**遗忘曲线调度：`js/scheduler.js` 只认 `window.POEMS_ALL`（242 首），
小古文放在 `window.POEMS_CLASSIC`，两边数据互不污染，所以每日 5 首不会混进小古文。

## 🔊 生字注音与朗读

给小朋友解决两个卡点：**不认字**和**不会读**。

### 生字注音

- **在哪用**：课内诗词详情弹层、小古文整页阅读器，都有一个「标注拼音」按钮，点一下全篇逐字注音
- **怎么实现**：`<ruby>` 把拼音压在汉字上方，标点与换行原样保留
- **默认关闭**：注音会让长诗看起来变密，所以默认不标，手动开启；开启状态记在 `localStorage`（`poem_helper_pinyin_v1`）
- **数据来源**：`data/pinyin-table.js` 收录 **2515 个汉字**，覆盖课内 242 首 + 课外 100 篇里出现的**全部**汉字，构建期用 `pinyin-pro` 离线生成，运行时**不依赖任何第三方库**
- **多音字**：值含斜杠表示多音（如 `曲:qū/qǔ`），`js/pinyin.js` 按上下文选读音：
  1. 先查内置词表（`曲项`→qū、`绿水`→lǜ、`一水间`→jiān、`应怜`→yīng、`知者乐水`→yào …）
  2. 再看位置与邻字（如「不」在去声前变调读 `bú`）
  3. 都判定不了时取第一个读音（也是现代最常用读音）

> 拼音表是**只读数据**，不改动诗词正文，正文里不会混入拼音字符。

### 朗读

- **在哪用**：同样两个界面，「朗读全文」/「朗读」按钮，点一下读标题 + 朝代 + 作者 + 正文，再点停止
- **怎么实现**：浏览器内置的 Web Speech API（`speechSynthesis`），**零音频文件、零流量**，语音包在手机本机，**断网也能读**
- **中文优先**：自动挑 `zh-CN` 语音；语速 0.85 倍，比正常稍慢方便跟读
- **不支持时**：按钮直接禁用并显示「不支持朗读」，点了不会有反应，不会静默失败
- **iOS 注意**：Safari 要求由用户手势触发，所以是点击才读，不做自动播放

### 总开关

设置里新增「阅读辅助」开关，控制默认是否开启这套能力。
关闭后按钮仍在，只是不默认打开，避免小朋友误触。

## 🧪 测试

```bash
bash test/run.sh      # 全部测试
npm test              # 同上
```

包含七部分：

| 测试 | 内容 |
|---|---|
| `test/scheduler.test.js` | 记忆曲线调度算法断言（间隔序列、升降级、模糊、跨年级复习、7 种背诵范围、持久化等） |
| `test/ui.test.js` | jsdom 模拟浏览器的端到端断言（年级/学期切换、背诵范围切换、5 首计划、SVG 图标、文案、小古文入口、点击展开、评价后进度更新、用户名改标题等） |
| `test/classic.test.js` | 课外小古文：100 篇数据完整性、需求篇目齐备、不污染每日计划、搜索/筛选、整页阅读器、注音与朗读按钮、字号/译文、已读标记与持久化 |
| `test/helper.test.js` | 注音与朗读：拼音表覆盖全部汉字、多音字上下文判定（曲/绿/还/间/应/不/行/乐）、ruby 渲染与转义、无语音环境下的降级、有语音时真正发起朗读 |
| `test/username.test.js` | 用户名专项：带初始 `localStorage` 重启应用，校验刷新后保持、旧版设置兼容、注入防护 |
| `test/theme.test.js` | 主题专项：文案（不再把诗叫「歌」）、Web Font 自托管与生效元素、宋代传统色、favicon 尺寸与矢量绘制 |
| `test/pwa.test.js` | **真实浏览器**校验 iOS / PWA 能力（见下） |

关于 PWA 测试：

- 需要真实浏览器，依赖 `puppeteer`；未安装时自动跳过，不影响其它测试
- 覆盖 iPhone Safari、iPad、微信内置浏览器、安卓 Chrome、桌面 Chrome 五种环境
- 校验 iOS meta 标签、四种 `apple-touch-icon` 尺寸是否可访问、Service Worker 注册、**断网后主页面与小古文页都能打开**、**中文字体断网仍可用**、诗题计算样式确实命中 `Poem Serif SC`、引导条按平台正确显示、弹层按钮是否被遮挡等共 36 项

单独运行（需先起服务）：

```bash
npm i -D puppeteer
node scripts/serve.js &
node test/pwa.test.js
```

## 📁 目录结构

```
.
├── index.html              # 应用入口（含 iOS meta 标签）
├── classic.html            # 课外必背小古文学习库（整页阅读器）
├── manifest.webmanifest    # PWA 清单：应用名、图标、主题色
├── sw.js                   # Service Worker：离线缓存
├── css/
│   ├── style.css           # 样式（移动端优先 / 响应式 / iOS 安全区适配）
│   └── classic.css         # 小古文列表 + 整页阅读器样式
├── js/
│   ├── storage.js          # localStorage 进度持久化 + 备份导入导出
│   ├── scheduler.js        # 遗忘曲线调度算法 + 背诵范围（核心）
│   ├── app.js              # UI 渲染与交互（含用户名 → 标题联动）
│   ├── classic.js          # 小古文列表 / 搜索 / 阅读器 / 注音 / 朗读 / 已读标记
│   ├── manifest-loader.js  # 读取 PWA 清单，供用户名改名时动态生成
│   └── pwa.js              # PWA 注册 + iOS「添加到主屏幕」引导
├── fonts/                  # 自托管中文 Web Font（子集化，共约 1.3 MB）
│   ├── NotoSerifSC-400.woff2  # 思源宋体 常规：诗词标题/朝代/作者/正文
│   ├── NotoSerifSC-600.woff2  # 思源宋体 半粗：诗词标题
│   ├── NotoSansSC-400.woff2   # 思源黑体 常规：界面文字
│   └── NotoSansSC-600.woff2   # 思源黑体 半粗：按钮/标签
├── icons/
│   ├── icon.svg            # 矢量源图（宣纸底 + 天青圆窗 + 手绘「诗」字 + 朱砂印）
│   ├── icon-120 … 512.png  # PWA / 安卓图标
│   ├── icon-maskable-512.png
│   └── apple-touch-icon.png  # iOS 主屏幕图标
├── scripts/
│   └── serve.js            # 本地静态服务器
├── data/
│   ├── poems-1.js ~ poems-12.js   # 各年级诗词数据
│   ├── poems-classic.js    # 课外必背小古文 100 篇（原文 + 译文）
│   ├── pinyin-table.js     # 2515 个汉字拼音表（构建期离线生成，多音字标多个读音）
│   └── index.js            # 汇总索引与查询方法
└── test/
    ├── run.sh
    ├── scheduler.test.js
    ├── ui.test.js
    ├── username.test.js    # 用户名：刷新保持 / 旧版兼容 / 注入防护
    ├── theme.test.js       # 主题：文案 / 字体 / 配色 / favicon
    └── pwa.test.js         # iOS / 多端兼容测试（真实浏览器）
```

## 🔧 扩展数据

在 `data/poems-N.js` 中按以下结构追加即可，`id` 需唯一：

```js
{
  id: "xx1-13",
  title: "诗题",
  author: "作者",
  dynasty: "唐",
  grade: 1,      // 1-12
  term: 1,       // 1 上学期 / 2 下学期
  text: "第一句，\n第二句。"
}
```

## 🔧 扩展小古文

在 `data/poems-classic.js` 的数组里追加即可，`id` 用 `gw-` 前缀保证唯一：

```js
{
  id: "gw-35",
  title: "篇名",
  source: "《出处》",
  dynasty: "唐",
  author: "作者",
  gradeGroup: "课外必背",   // 分组标题，决定列表分组
  difficulty: 1,            // 1 易 / 2 中 / 3 难，留给后续做难度筛选
  text: "第一句，\n第二句。",
  translation: "白话译文。"
}
```

## 🎨 界面主题 · 宋代传统色 + 中文 Web Font

### 配色：以宋代取色

整套配色取自**宋代**（汝窑天青 / 宣纸 / 秋香 / 琥珀 / 朱砂 / 缃色），不再用常见的"草木绿"：

| 变量 | 传统色 | 色值 | 用在哪 |
|---|---|---|---|
| `--green` | 雨过天青（汝窑） | `#4d7d74` | 主色：今日任务卡、主按钮、选中态 |
| `--green-dark` | 深天青 | `#3a6159` | 悬停 / 强调 |
| `--bg` | 素绢（米灰） | `#efe7d7` | 页面底色（叠了一层极淡的天青/琥珀晕染） |
| `--card` | 宣纸 | `#fdfaf2` | 卡片、弹层 |
| `--amber` | 琥珀 | `#c07a3a` | 复习标签、模糊按钮 |
| `--red` | 朱砂 | `#b8473d` | 忘记按钮 |
| `--gold` | 缃色 | `#d9b566` | 今日进度环 |
| `--blue` | 天水碧 | `#5c7f96` | 小古文入口 |
| `--ink` | 墨 | `#322a24` | 正文 |
| `--line` | 界行 | `#e3d8c2` | 分隔线、描边 |

### 字体：自托管思源宋体 / 黑体

- **诗词的标题、朝代、作者、正文**统一使用 **Noto Serif SC（思源宋体）**——宋体本就源于宋代刻本，横细竖粗、笔画有顿挫，最贴合线装书里的诗
- 其余界面文字用 **Noto Sans SC（思源黑体）**打头
- 字体放在 `fonts/` 目录**自托管**，不请求任何第三方 CDN：
  - 按本站实际用到的 **3031 个字形**做了子集化，四个文件共约 **1.3 MB**
  - 断网可用（已进 Service Worker 预缓存清单），苹果 / 安卓 / Windows / Linux 渲染一致
  - 缺字自动回退到系统宋体/黑体，不会出现空心方块
- 字体栈写在 `--font-poem` / `--font-ui` 两个变量里，改一处即可换全站

### favicon

重新设计为矢量图标，所有尺寸从 `icons/icon.svg` 统一生成：

- **宣纸底**+缃色内界行（宋代刻本的双边栏）
- **天青圆窗**里一个手绘笔画的「**诗**」字（矢量路径，不依赖任何字体）
- 右下角一方**朱砂小印**
- `icon-maskable-512.png` 单独做了内容缩进，被系统裁成圆形 / 水滴形时不会切到字或印

## 💡 设计说明

- **为什么要"不点击就只显示诗名"**：先给孩子回忆的机会，避免直接看到正文产生依赖，符合"主动回忆"的记忆原理。
- **为什么要如实评价掌握程度**：记忆曲线的排期完全依赖真实反馈。忘记却选"记住"，会导致间隔拉得过长而彻底遗忘。
- **为什么跨年级复习**：已经学过的诗即使升了年级也要按期复习，因此到期诗会在任意年级的任务列表中出现，保证不遗忘。
- **为什么默认只背本册**：先跟着课内进度走，需要时再用「背诵范围」扩到之前学期或整段学段。
- **为什么小古文单独开一个页面**：篇幅长（手机上卡片弹窗装不下）+ 目标不同（读懂 ≠ 背熟）+ 不想占用每日任务名额，所以独立成 `classic.html`，只列出来点击学习，读完打个「已读」就好。

## 🔒 关于数据与隐私

- 所有学习进度与用户名只存在**本机浏览器**（localStorage），不上传任何服务器
- 无账号、无埋点、无第三方请求，断网可用
- 想在手机和电脑之间同步，可用设置里的「导出备份 / 导入备份」

## 📄 License

MIT
